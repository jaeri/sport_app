export interface Team {
    _id: string;
    name: string;
  }
  
  export interface Player {
    _id: string;
    name: string;
  }
  
  export interface LoginResponse {
    token: string;
  }